# v1.0.0 릴리스 계획

> 상태: **범위 확정 전** — 신규 피처 목록을 채우는 중. v1.0.0 의 단일 기준 문서.
> 릴리스가 가까워지면 당일 절차를 `runbook.md` 로 분리한다 (지금은 4절 요약만).

## 왜 v1.0.0 인가

prod 는 v0.15.0(2025-07-12)에 멈춰 있고, 이후 1년치 개선이 전부 main(staging)에서 검증돼 왔다.
이번 릴리스는 **쌓인 것 전부 + 신규 피처 묶음**을 한 번에 내보내는 메이저 릴리스다.
릴리스가 늦어질수록 변경 뭉치는 더 커진다 — 신규 피처 범위를 빨리 닫는 것이 리스크 관리다.

## 1. 이미 main 에 있는 것 — 릴리스하면 자동 포함

상세·PR 번호는 각 레포 backlog "완료" 절. 여기서는 사용자 눈에 보이는 단위만.

- **말씀카드**: 생성·재생성/교체, 일일 한도, 카카오 공유 보상(+1회), 안내 시트, 히스토리 드로워
- **QT**: 페이지 개편(구절 픽커·범위 선택), LLM 일일 한도
- **공지 시스템**: DB 공지 + 전역 모달 + 목록 페이지, 어드민 저작·게시 절차(레포 원고)
- **어드민**: 대시보드 전면 개편(지표·퍼널), `is_admin` 접근 제어
- **성경 본문**: 원본(goodtv) 동기화 — 5,538행 교정, 누락 절 복구
- **스토리지**: 신규 업로드 R2 전환 (release 시 prod 자동 활성화 — 환경변수 등록 완료)
- **회원 탈퇴**: 소프트 삭제 전환(그룹장 이양 포함) — 기존엔 탈퇴가 조용히 실패했다
- **기반**: 뷰포트 dvh/safe-area, Toast UX, JWT 서명 검증, 죽은 엔드포인트 제거, `is_admin` 자기부여 차단

## 2. 신규 피처 — 🔴 범위 미정 (이 문서의 핵심 공란)

| 피처 | 상태 | 계획서 |
|---|---|---|
| _(사용자가 계획 중인 피처들 — 목록 받는 대로 채운다)_ | 미정 | — |

피처마다 별도 계획서(`plans/<피처>.md` 또는 하위 문서가 생기면 폴더)를 만들고 여기엔 한 줄 + 링크만.

**backlog 에 있던 후보** (v1 에 넣을지 각각 판단):

| 후보 | 출처 | 비고 |
|---|---|---|
| 그룹장 이양 알림 | 탈퇴 후속 | 알림 설계 필요 |
| 공지 열람 기록 계정 단위 | 개선 아이디어 | 현재 localStorage — 기기 바꾸면 재노출 |
| 구절 픽커 초성 키보드 검색 | 개선 아이디어 | |
| 신고 접수 테이블화 | 개선 아이디어 | 현재 Discord 웹훅뿐 |
| 공유 랜딩 문서 스크롤 모드 | 개선 아이디어 | iOS Safari 툴바 이슈 |

## 3. 보안 — v1 에 포함할 것 (결정 필요)

[security-backlog.md](../../security-backlog.md) 권장 순서: **8 → 2 → 1 → 4 → 3**

| # | 항목 | 제안 |
|---|---|---|
| 8 | `premium_expired_at` 자기부여 차단 | ✅ **완료** ([Api#59](https://github.com/TeamVisioneer/PrayU-Api/pull/59) · [#497](https://github.com/TeamVisioneer/PrayU-Web/pull/497)) |
| 2 | Kakao client secret 서버 이전 | ❌ **v1 제외 확정** (2026-07-31) — 인증 흐름 전체를 건드린다. `handle_new_user` 트리거 제거와 묶어 릴리스 후 |
| 1 | RLS 전면 정비 (그룹 격리) | **릴리스 직후 트랙** — 파급이 커서 릴리스를 지연시킬 리스크 |
| 4 | 어드민 이메일 하드코딩 제거 | 1과 함께 |
| 3 | service_role 키 로테이션 | 1·2 마무리 후 일괄 (prod cron 하드코딩과 동시 갱신) |

근거: RLS 구멍은 **이미 prod 에 있다**(baseline = prod 스키마 사본). v1 릴리스가 보안을 악화시키지 않으므로
릴리스를 보안 정비의 인질로 잡지 않는다.

## 4. 릴리스 절차 (요약)

1. **staging 최종 인수** — 남은 것: 실기기 뷰포트·앱 WebView 회귀, 탈퇴 흐름 실계정 1회
2. **Api 태그 발행** → prod 마이그레이션 + 함수 배포
3. **운영 조작** (사람) — 🔴 관리자 `is_admin=true` · 카카오 웹훅 등록(POST) · `push` 함수 삭제 ·
   `KAKAO_ADMIN_KEY`·도메인 확인 — 전체 목록: [Api backlog "운영 조작 대기"](../../../../PrayU-Api/docs/backlog.md)
4. **web 태그 발행** → Vercel prod 빌드 (R2 여기서 켜진다)
5. **릴리스 후** — 공지 게시(레포 원고 준비됨) · prod 탈퇴 시도 이력 확인 · Sentry/analytics 모니터링

## 5. App 트랙 (결정 필요)

딥링크·푸시 수리 계획([PrayU-App/docs/plans/deeplink-push-review-plan.md](../../../../PrayU-App/docs/plans/deeplink-push-review-plan.md))이 보류 중.

- 웹 v1.0.0 은 **앱 업데이트 없이 완결**된다 — WebView 셸이라 웹 배포가 곧 앱 내 반영
- 앱 스토어 릴리스는 심사 리드타임이 있어 **별도 트랙 권장**

## 6. 결정 대기

- [ ] 신규 피처 목록 (2절 공란)
- [x] ~~보안 8·2번 v1 포함 여부~~ — 8번 포함(완료) · 2번 제외 (2026-07-31)
- [ ] App 트랙 분리 여부
- [ ] 목표 시점
